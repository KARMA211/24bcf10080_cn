subnet -> kaha jara hia(network adress) 
            255.255.255.0
            agla router konsa hia(first router encountered ) 
            


interface (FastEtch ) ->
                        ip addr -  the ip wire is connected to 
                        subnet mask-> 255.255.255.0
                



RIP->
    just type the networs cnnected and press enter 






data link layer -?
    RESPONSIBILTY OF DLL
    SUBLAYER-> LLC AND MAC 
    ERROR DETECTION 
        PARITY 
        CHECKSUM 
        CRC 
    RANDOM ACESS PROTOCOL : CHANNEL - CHANCE OF COLLISON 
        ALOHA 
            PURE ALOHA 
            SLOTTED ALOHA 
        CSMA 
            CSMA/CD (WEIRED)
            CSMA/CA (WIRELESS)


ERROR DETECTION (go through the ppt ) 
    HAMMING CODE
        -parity 






-enable 
-show vlan brief
-conf t
-vlan 10 (can give it any serial no...anything except vlan 1) as vlan 1 is already default 
-name nameYOUwant
-exit



now u gotta config the port to the vlan 
-interface fa0/1
-switchport mode access vlan 10
and then exit twice to go into normal mode and do show vlan brief to see the vlans 

for config of trunk 
-interface gigabitEthernet 0/1
-switchport mode trunk 
